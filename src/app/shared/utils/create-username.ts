export class CreateUsername {

    /**
     * Recebe nome e cria username dentro do formato adotado
     * @param name
     * @returns username
     */
  static transformNameToUsername(name: string) {
    const username = '';

    return username;
  }
}
